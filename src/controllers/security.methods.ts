import dotenv from "dotenv";
import { Request, Response } from "express";

//Enviroment Variables
dotenv.config();

export const set_pinCode = async (req: Request, res:Response)=>{

}
export const set_secretQuestions = async (req: Request, res:Response)=>{

}